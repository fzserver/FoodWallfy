import 'package:flutter/material.dart';

class FzGradients {
  FzGradients._();

  static var bluePinkGradient = LinearGradient(
    colors: [
      const Color(0xFFCC0099),
      const Color(0xFF6600FF),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.0,
      1.0,
    ],
  );

  static var blueGreenGradient = LinearGradient(
    colors: [
      const Color(0xFF0099CC),
      const Color(0xFF99FF66),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var yellowOrangeGradient = LinearGradient(
    colors: [
      const Color(0xFFFFFF00),
      const Color(0xFFFF6600),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var pinkRedGradient = LinearGradient(
    colors: [
      const Color(0xFFFF3399),
      const Color(0xFFFF0000),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.0,
      1.0,
    ],
  );

  static var blackBlueGradient = LinearGradient(
    colors: [Colors.blueGrey.shade800, Colors.black87],
    tileMode: TileMode.clamp,
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var redSexyGradient = LinearGradient(
    colors: [
      const Color(0xFFF6356F),
      const Color(0xFFFF5F50),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var greenSexyGradient = LinearGradient(
    colors: [
      const Color(0xFF92D000),
      const Color(0xFFE1EB01),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var violetSexyGradient = LinearGradient(
    colors: [
      const Color(0xFF52A7EA),
      const Color(0xFF712E98),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var blueSexyGradient = LinearGradient(
    colors: [
      const Color(0xFF46A3B7),
      const Color(0xFF86F1DE),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var orangeSexyGradient = LinearGradient(
    colors: [
      const Color(0xFFFF9B30),
      const Color(0xFFF8FF3F),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var thodaSexyGradient = LinearGradient(
    colors: [
      const Color(0xFFF58573),
      const Color(0xFF5F79F4),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var skyBlueGradient = LinearGradient(
    colors: [
      const Color(0xFF15EDED),
      const Color(0xFF029CF5),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
  static var blackSexyGradient = LinearGradient(
    colors: [
      const Color(0xFF90203F),
      const Color(0xFF537895),
    ],
    tileMode: TileMode.clamp,
    begin: Alignment.bottomLeft,
    end: Alignment.topRight,
    stops: [
      0.0,
      1.0,
    ],
  );
}
